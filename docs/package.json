{
  "name": "@rovy/docs",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "node ../scripts/build-parallel-report.mjs && vitepress dev",
    "build": "node ../scripts/build-parallel-report.mjs && vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
